package pen;

// Same refill behaviour as ball pen
public class GelPen extends Pen {
    public GelPen(String color, boolean hasCap, boolean hasClip) {
        super("gel", color, hasCap, hasClip);
    }
}